.logo
  .highlight
    =link_to "Contact Management APP", root_path
.navigation
  -if user_signed_in?
    =link_to "Çıkış", destroy_user_session_path, :method => :delete
  -else
    =link_to "Sign Up", new_user_registration_path
    =link_to "Sign In", new_user_session_path